# chapter-14 — Reactive with WebFlux: an honest benchmark

This chapter snapshot is the baseline CinéTrack application used by the chapter.
The chapter's value is in the JVM/profiling/runtime configuration and the
walkthrough in the book, not in different application code. Application sources
are unchanged from `chapter-13`; this directory exists so `cd chapter-14` in the book's
commands works and so chapter-specific runtime configs, scripts, and notes have
a place to live.

See the corresponding chapter in the book for the full walkthrough.
